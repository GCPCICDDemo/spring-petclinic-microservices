# visits-service-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  namespace: ${NAMESPACE}
spec:
  replicas: ${REPLICAS}
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      containers:
      - name: visits-service
        image: asia-northeast1-docker.pkg.dev/demoforgaudiy/docker-local/spring-petclinic-visits-service:${VISITS_SERVICE_VERSION}
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: ${CPU_LIMIT}
            memory: ${MEMORY_LIMIT}
          requests:
            cpu: ${CPU_REQUEST}
            memory: ${MEMORY_REQUEST}
---
apiVersion: v1
kind: Service
metadata:
  name: visits-service
  namespace: ${NAMESPACE}
spec:
  selector:
    app: visits-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

